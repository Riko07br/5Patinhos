
public enum DuckState {
    Stop = 0,       // Param de se mover
    Patrol = 1,     // Ficam andando por pontos aleatorios predefinidos na tela
    Playing = 2,    // Saem pelas bordas da tela
    FollowMom = 3,
}
